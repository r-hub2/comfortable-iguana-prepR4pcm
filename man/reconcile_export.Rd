% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_export.R
\name{reconcile_export}
\alias{reconcile_export}
\title{Write an aligned dataset, tree, and mapping table to disk}
\usage{
reconcile_export(
  reconciliation,
  data = NULL,
  tree = NULL,
  species_col = NULL,
  dir = tempfile("prepR4pcm-export-"),
  prefix = "reconciled",
  tree_format = c("nexus", "newick"),
  drop_unresolved = TRUE
)
}
\arguments{
\item{reconciliation}{A \link{reconciliation} object returned by
\code{\link[=reconcile_tree]{reconcile_tree()}}, \code{\link[=reconcile_data]{reconcile_data()}}, or a related matcher.}

\item{data}{A data frame to align. If \code{NULL}, only the tree and mapping
are written.}

\item{tree}{An \code{ape::phylo} object or file path. If \code{NULL}, only the
data and mapping are written.}

\item{species_col}{A length-1 character vector. Column name in \code{data}
containing species names. Auto-detected when \code{NULL}.}

\item{dir}{A length-1 character vector. Path to the output directory
that will receive the exported files (e.g. a project's
\verb{data-output/} folder, or a staging directory before a Zenodo
deposit). Created if it does not exist. By default, a unique
temporary directory is used so the function does not write to the
current working directory unless you explicitly request it.}

\item{prefix}{A length-1 character vector. File name prefix. Default
\code{"reconciled"}.}

\item{tree_format}{A length-1 character vector. Tree output format:
\code{"nexus"} (default) or \code{"newick"}.}

\item{drop_unresolved}{Logical. Drops unresolved species when \code{TRUE}.
Default \code{TRUE}.}
}
\value{
A named list of file paths (invisibly):
\verb{$data} (CSV), \verb{$tree} (Nexus or Newick), \verb{$mapping} (CSV), and
\verb{$unused_overrides} (CSV; \code{NULL} when there are no rejected
overrides on the reconciliation).
}
\description{
Apply a reconciliation and save three files: the aligned CSV, the
pruned tree, and the full mapping tibble. Intended for producing
analysis-ready, archivable outputs --- drop the three files into a
Zenodo deposit or a project's \verb{data-output/} folder alongside the
reconciliation report and you have a fully documented provenance
trail.
}
\examples{
data(avonet_subset)
data(tree_jetz)
result <- reconcile_tree(avonet_subset, tree_jetz,
                         x_species = "Species1", authority = NULL)
out_dir <- tempfile("export_")
files <- reconcile_export(result,
                          data = avonet_subset, tree = tree_jetz,
                          species_col = "Species1",
                          dir = out_dir, prefix = "avonet_jetz")
files$data     # path to CSV
files$tree     # path to Nexus tree
files$mapping  # path to mapping CSV
unlink(out_dir, recursive = TRUE)  # clean up

}
\seealso{
\code{\link[=reconcile_apply]{reconcile_apply()}} for in-memory alignment without writing
to disk; \code{\link[=reconcile_report]{reconcile_report()}} for a self-contained HTML audit
trail.

Other reconciliation functions:
\code{\link[=reconcile_apply]{reconcile_apply()}},
\code{\link[=reconcile_augment]{reconcile_augment()}},
\code{\link[=reconcile_crosswalk]{reconcile_crosswalk()}},
\code{\link[=reconcile_crosswalk_supplement]{reconcile_crosswalk_supplement()}},
\code{\link[=reconcile_data]{reconcile_data()}},
\code{\link[=reconcile_diff]{reconcile_diff()}},
\code{\link[=reconcile_mapping]{reconcile_mapping()}},
\code{\link[=reconcile_merge]{reconcile_merge()}},
\code{\link[=reconcile_multi]{reconcile_multi()}},
\code{\link[=reconcile_override]{reconcile_override()}},
\code{\link[=reconcile_override_batch]{reconcile_override_batch()}},
\code{\link[=reconcile_plot]{reconcile_plot()}},
\code{\link[=reconcile_report]{reconcile_report()}},
\code{\link[=reconcile_review]{reconcile_review()}},
\code{\link[=reconcile_splits_lumps]{reconcile_splits_lumps()}},
\code{\link[=reconcile_suggest]{reconcile_suggest()}},
\code{\link[=reconcile_summary]{reconcile_summary()}},
\code{\link[=reconcile_to_trees]{reconcile_to_trees()}},
\code{\link[=reconcile_tree]{reconcile_tree()}},
\code{\link[=reconcile_trees]{reconcile_trees()}}
}
\concept{reconciliation functions}
