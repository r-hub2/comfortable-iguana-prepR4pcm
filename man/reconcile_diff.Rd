% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_diff.R
\name{reconcile_diff}
\alias{reconcile_diff}
\title{Diff two reconciliations to see what changed}
\usage{
reconcile_diff(x, y, quiet = FALSE)
}
\arguments{
\item{x}{A \link{reconciliation} object --- the "before" state.}

\item{y}{A \link{reconciliation} object --- the "after" state. Must be
reconciled against the same \code{x} data so that \code{name_x} values are
comparable.}

\item{quiet}{Logical. Suppresses the console summary when \code{TRUE}.
Default \code{FALSE}.}
}
\value{
A list with the following components:
\describe{
\item{\code{gained}}{Tibble of species matched in \code{y} but unresolved
in \code{x}.}
\item{\code{lost}}{Tibble of species matched in \code{x} but unresolved in
\code{y}.}
\item{\code{type_changed}}{Tibble of species whose \code{match_type}
differs between the two runs.}
\item{\code{target_changed}}{Tibble of species whose \code{name_y}
differs.}
\item{\code{unused_overrides_diff}}{Tibble of overrides that are in
the \code{unused_overrides} slot of one reconciliation but not
the other; columns \code{name_x}, \code{name_y}, \code{reason}, \code{side}
(\code{"x"} or \code{"y"}).}
\item{\code{summary}}{A one-row tibble with counts: \code{n_gained},
\code{n_lost}, \code{n_type_changed}, \code{n_target_changed}, \code{n_shared},
\code{n_unused_override_diff}.}
}
}
\description{
Compare a "before" and "after" \link{reconciliation} and list every
species whose outcome differs: newly matched, newly unresolved,
promoted to a higher-confidence match type, or linked to a different
target. Useful for:
\itemize{
\item checking the effect of adding a taxonomy crosswalk or a
batch of manual overrides,
\item comparing two taxonomic authorities (e.g. Catalogue of Life
vs GBIF),
\item auditing changes between runs before and after tightening
the fuzzy threshold.
}
}
\examples{
x <- data.frame(species = c("A a", "A old", "B c"))
tree <- ape::read.tree(text = "((A_a:1,A_new:1):1,B_c:2);")

# Without manual overrides
r1 <- reconcile_tree(x, tree, x_species = "species",
                     authority = NULL, quiet = TRUE)

# With one manual override
overrides <- data.frame(name_x = "A old", name_y = "A new",
                        match_type = "manual")
r2 <- reconcile_tree(x, tree, x_species = "species",
                     authority = NULL, overrides = overrides,
                     quiet = TRUE)

d <- reconcile_diff(r1, r2, quiet = TRUE)
cat("Gained:", nrow(d$gained), "| Lost:", nrow(d$lost), "\n")

}
\seealso{
\code{\link[=reconcile_crosswalk]{reconcile_crosswalk()}} for building an override table from
a published taxonomy crosswalk; \code{\link[=reconcile_override_batch]{reconcile_override_batch()}} for
applying many hand edits.

Other reconciliation functions:
\code{\link[=reconcile_apply]{reconcile_apply()}},
\code{\link[=reconcile_augment]{reconcile_augment()}},
\code{\link[=reconcile_crosswalk]{reconcile_crosswalk()}},
\code{\link[=reconcile_crosswalk_supplement]{reconcile_crosswalk_supplement()}},
\code{\link[=reconcile_data]{reconcile_data()}},
\code{\link[=reconcile_export]{reconcile_export()}},
\code{\link[=reconcile_mapping]{reconcile_mapping()}},
\code{\link[=reconcile_merge]{reconcile_merge()}},
\code{\link[=reconcile_multi]{reconcile_multi()}},
\code{\link[=reconcile_override]{reconcile_override()}},
\code{\link[=reconcile_override_batch]{reconcile_override_batch()}},
\code{\link[=reconcile_plot]{reconcile_plot()}},
\code{\link[=reconcile_report]{reconcile_report()}},
\code{\link[=reconcile_review]{reconcile_review()}},
\code{\link[=reconcile_splits_lumps]{reconcile_splits_lumps()}},
\code{\link[=reconcile_suggest]{reconcile_suggest()}},
\code{\link[=reconcile_summary]{reconcile_summary()}},
\code{\link[=reconcile_to_trees]{reconcile_to_trees()}},
\code{\link[=reconcile_tree]{reconcile_tree()}},
\code{\link[=reconcile_trees]{reconcile_trees()}}
}
\concept{reconciliation functions}
