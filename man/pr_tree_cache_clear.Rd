% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pr_tree_cache.R
\name{pr_tree_cache_clear}
\alias{pr_tree_cache_clear}
\title{Clear the local tree-retrieval cache}
\usage{
pr_tree_cache_clear(confirm = TRUE, source = NULL)
}
\arguments{
\item{confirm}{Logical. Ask interactively before deleting? Default
\code{TRUE}. Ignored in non-interactive sessions (deletion proceeds).}

\item{source}{A length-1 character vector or \code{NULL}. If non-\code{NULL},
only entries from that backend are cleared (e.g. \code{"datelife"} to
wipe only datelife cache after a database refresh). If \code{NULL}
(default), all entries are cleared.}
}
\value{
Invisibly, the number of files removed.
}
\description{
Removes all cached \code{pr_get_tree()} / \code{pr_date_tree()} results. By
default asks for confirmation before deleting; pass \code{confirm = FALSE}
to skip the prompt (useful in scripts).
}
\examples{
# Demo against a throwaway cache so the user's real cache is untouched
old_opt   <- getOption("prepR4pcm.cache_dir")
tmp_cache <- file.path(tempdir(), "prepR4pcm-cache-demo")
pr_tree_cache_dir(tmp_cache)

# Drop two dummy entries so there is something to clear:
dir.create(file.path(tmp_cache, "fishtree"), showWarnings = FALSE)
dir.create(file.path(tmp_cache, "rotl"),     showWarnings = FALSE)
saveRDS(NULL, file.path(tmp_cache, "fishtree", "abc.rds"))
saveRDS(NULL, file.path(tmp_cache, "rotl",     "def.rds"))

pr_tree_cache_status()                # 2 entries
pr_tree_cache_clear(confirm = FALSE)  # removes both
pr_tree_cache_status()                # empty

# Restore the previous cache directory
options(prepR4pcm.cache_dir = old_opt)

}
\seealso{
\code{\link[=pr_tree_cache_dir]{pr_tree_cache_dir()}} / \code{\link[=pr_tree_cache_status]{pr_tree_cache_status()}}.
}
