% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_override_batch.R
\name{reconcile_override_batch}
\alias{reconcile_override_batch}
\title{Apply many manual corrections to a reconciliation at once}
\usage{
reconcile_override_batch(reconciliation, overrides, quiet = FALSE)
}
\arguments{
\item{reconciliation}{A \link{reconciliation} object returned by
\code{\link[=reconcile_tree]{reconcile_tree()}}, \code{\link[=reconcile_data]{reconcile_data()}}, or a related matcher.}

\item{overrides}{A data frame, or a length-1 character vector giving
the path to a CSV file with the same columns:
\describe{
\item{\code{name_x} (required)}{The original name in source \code{x}
(your data).}
\item{\code{action}}{One of \code{"accept"} (default), \code{"reject"},
\code{"replace"}. See \code{\link[=reconcile_override]{reconcile_override()}} for the semantics.}
\item{\code{name_y}}{The target name in source \code{y}; required for
\code{"accept"} and \code{"replace"}.}
\item{\code{note}}{Optional free-text justification.}
}}

\item{quiet}{Logical. Suppresses per-override success messages when
\code{TRUE}. Default \code{FALSE}.}
}
\value{
An updated \link{reconciliation} object with all overrides
applied.
}
\description{
A convenience wrapper around \code{\link[=reconcile_override]{reconcile_override()}} for curated
batches of manual decisions.
}
\details{
Typical workflow: generate a CSV of corrections (by hand, or with
the help of \code{\link[=reconcile_suggest]{reconcile_suggest()}}), check it into version control,
and apply it on every run so the corrections are reproducible and
reviewable. This is also the safer way to use a taxonomy crosswalk as
a supplement: run a baseline reconciliation first, then apply reviewed
one-to-one crosswalk rows only to names that remain unresolved. Passing
a crosswalk through the \code{overrides} argument of \verb{reconcile_*()} applies
those rows before the cascade, so they can preempt exact or normalised
matches. For the common crosswalk-supplement workflow, use
\code{\link[=reconcile_crosswalk_supplement]{reconcile_crosswalk_supplement()}} so duplicate source or target
candidates are skipped instead of being resolved by row order.
}
\examples{
data(avonet_subset)
data(tree_jetz)
result <- reconcile_tree(avonet_subset, tree_jetz,
                         x_species = "Species1", authority = NULL)
# Create a batch of overrides
batch <- data.frame(
  name_x = reconcile_mapping(result)$name_x[
    reconcile_mapping(result)$match_type == "unresolved" &
    reconcile_mapping(result)$in_x][1:2],
  name_y = tree_jetz$tip.label[1:2],
  action = "accept",
  note = "Batch demo",
  stringsAsFactors = FALSE
)
batch <- batch[!is.na(batch$name_x), ]
if (nrow(batch) > 0) {
  result2 <- reconcile_override_batch(result, batch)
}

}
\seealso{
\code{\link[=reconcile_override]{reconcile_override()}} for the single-override case;
\code{\link[=reconcile_crosswalk]{reconcile_crosswalk()}} for building an override table from a
published taxonomy crosswalk.

Other reconciliation functions:
\code{\link[=reconcile_apply]{reconcile_apply()}},
\code{\link[=reconcile_augment]{reconcile_augment()}},
\code{\link[=reconcile_crosswalk]{reconcile_crosswalk()}},
\code{\link[=reconcile_crosswalk_supplement]{reconcile_crosswalk_supplement()}},
\code{\link[=reconcile_data]{reconcile_data()}},
\code{\link[=reconcile_diff]{reconcile_diff()}},
\code{\link[=reconcile_export]{reconcile_export()}},
\code{\link[=reconcile_mapping]{reconcile_mapping()}},
\code{\link[=reconcile_merge]{reconcile_merge()}},
\code{\link[=reconcile_multi]{reconcile_multi()}},
\code{\link[=reconcile_override]{reconcile_override()}},
\code{\link[=reconcile_plot]{reconcile_plot()}},
\code{\link[=reconcile_report]{reconcile_report()}},
\code{\link[=reconcile_review]{reconcile_review()}},
\code{\link[=reconcile_splits_lumps]{reconcile_splits_lumps()}},
\code{\link[=reconcile_suggest]{reconcile_suggest()}},
\code{\link[=reconcile_summary]{reconcile_summary()}},
\code{\link[=reconcile_to_trees]{reconcile_to_trees()}},
\code{\link[=reconcile_tree]{reconcile_tree()}},
\code{\link[=reconcile_trees]{reconcile_trees()}}
}
\concept{reconciliation functions}
