% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pr_get_tree_status.R
\name{pr_get_tree_status}
\alias{pr_get_tree_status}
\title{Report the install status of every \code{pr_get_tree()} backend}
\usage{
pr_get_tree_status(check_network = FALSE)
}
\arguments{
\item{check_network}{Logical. Should the probe attempt a tiny
network call to test that backends needing the network are
actually reachable? Default \code{FALSE} (purely local check, no
side effects). Set \code{TRUE} to also test reachability --- adds
1-3 seconds and requires internet.}
}
\value{
A \code{data.frame} with one row per backend and columns:
\describe{
\item{\code{source}}{Backend name, as passed to \code{pr_get_tree()}.}
\item{\code{installed}}{Logical --- is the package available?}
\item{\code{version}}{Character --- installed version, or \code{NA}.}
\item{\code{needs_network}}{Logical --- does the backend hit a
remote server at runtime?}
\item{\code{reachable}}{Logical or \code{NA} --- result of the network
check (only populated when \code{check_network = TRUE}).}
\item{\code{install_hint}}{Character --- the install command to run
when \code{installed = FALSE}.}
\item{\code{source_repo}}{Character --- "CRAN" or a GitHub
repo for non-CRAN backends.}
}
}
\description{
Walks every backend supported by \code{\link[=pr_get_tree]{pr_get_tree()}} and reports
whether the underlying package is installed (and at what version),
whether it requires network, and what to do if it's missing. Useful
for first-time users figuring out which backends are available, and
for CI sanity checks.
}
\examples{
# Local-only probe (fast, no network)
pr_get_tree_status()

\donttest{
  # Also test reachability of remote backends
  pr_get_tree_status(check_network = TRUE)
}

}
\seealso{
\code{\link[=pr_get_tree]{pr_get_tree()}} / \code{\link[=pr_date_tree]{pr_date_tree()}} for the consumers.
}
