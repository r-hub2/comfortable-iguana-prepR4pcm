% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pr_tree_cache.R
\name{pr_tree_cache_dir}
\alias{pr_tree_cache_dir}
\title{Get or set the local tree-retrieval cache directory}
\usage{
pr_tree_cache_dir(path = NULL)
}
\arguments{
\item{path}{A length-1 character vector or \code{NULL}. If non-\code{NULL},
sets the cache directory to \code{path} (creating it if it doesn't
exist). If \code{NULL} (default), returns the currently configured
directory.}
}
\value{
A length-1 character vector --- the absolute path of the
cache directory.
}
\description{
Returns the path to the cache directory used by \code{\link[=pr_get_tree]{pr_get_tree()}} and
\code{\link[=pr_date_tree]{pr_date_tree()}} when called with \code{cache = TRUE}. Pass a path to
override the default.
}
\details{
The default cache directory is \code{\link[tools:R_user_dir]{tools::R_user_dir()}} with type
\code{"cache"} and the package name \code{"prepR4pcm"}, which on Linux is
typically \verb{~/.cache/R/prepR4pcm/}, on macOS
\verb{~/Library/Caches/org.R-project.R/R/prepR4pcm/}, and on Windows
something under \verb{\%LOCALAPPDATA\%\\R\\cache\\R\\prepR4pcm\\}.

To use a cache directory you control, pass its path explicitly with
\code{pr_tree_cache_dir(path)}.
}
\examples{
# Default location
pr_tree_cache_dir()

old_cache <- getOption("prepR4pcm.cache_dir", NULL)
tmp_cache <- tempfile("prepR4pcm-cache-")
pr_tree_cache_dir(tmp_cache)
options(prepR4pcm.cache_dir = old_cache)
unlink(tmp_cache, recursive = TRUE)

}
\seealso{
\code{\link[=pr_tree_cache_status]{pr_tree_cache_status()}} / \code{\link[=pr_tree_cache_clear]{pr_tree_cache_clear()}};
\code{\link[=pr_get_tree]{pr_get_tree()}} for the consumer.
}
