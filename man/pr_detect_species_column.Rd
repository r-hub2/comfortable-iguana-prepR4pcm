% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pr_detect.R
\name{pr_detect_species_column}
\alias{pr_detect_species_column}
\title{Detect the species name column in a data frame}
\usage{
pr_detect_species_column(df, arg_name = "x_species")
}
\arguments{
\item{df}{A data frame.}

\item{arg_name}{Character. Name of the argument, used in error messages
(e.g., \code{"x_species"} or \code{"y_species"}).}
}
\value{
A length-1 character vector: the detected column name.
}
\description{
Uses a two-stage heuristic: first checks for common column names, then
falls back to content-based detection (binomial name pattern).
}
\details{
\strong{Stage 1 — Name matching.} Checks column names (case-insensitive) against
a priority list: \code{species}, \code{species_name}, \code{binomial}, \code{taxon},
\code{scientificName}, \code{Scientific_name}, \code{canonical_name}, \code{tip.label},
\code{PhyloName}, \code{Binomial}, \code{latin_name}, \code{sci_name}.

\strong{Stage 2 — Content heuristic.} If no name match, checks which character
columns have >50\% of non-NA values matching the binomial pattern
\verb{^[A-Z][a-z]+ [a-z]+}.

If zero or multiple candidates are found, the function stops with an
informative error.
}
\keyword{internal}
