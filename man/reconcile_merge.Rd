% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_merge.R
\name{reconcile_merge}
\alias{reconcile_merge}
\title{Merge two reconciled datasets}
\usage{
reconcile_merge(
  reconciliation,
  data_x,
  data_y,
  species_col_x = NULL,
  species_col_y = NULL,
  how = c("inner", "left", "full"),
  suffix = c("_x", "_y"),
  drop_unresolved = FALSE
)
}
\arguments{
\item{reconciliation}{A \link{reconciliation} object (typically from
\code{\link[=reconcile_data]{reconcile_data()}}).}

\item{data_x}{The first data frame (source x in the reconciliation).}

\item{data_y}{The second data frame (source y in the reconciliation).}

\item{species_col_x}{A length-1 character vector. Species column in \code{data_x}.
Auto-detected if \code{NULL}.}

\item{species_col_y}{A length-1 character vector. Species column in \code{data_y}.
Auto-detected if \code{NULL}.}

\item{how}{A length-1 character vector. Join type:
\itemize{
\item \code{"inner"} (default): keep only species matched in both datasets.
\item \code{"left"}: keep all species from \code{data_x}.
\item \code{"full"}: keep all species from both datasets.
}}

\item{suffix}{A length-2 character vector. Suffixes to disambiguate columns with the
same name in both datasets. Default \code{c("_x", "_y")}.}

\item{drop_unresolved}{Logical. If \code{TRUE}, rows where \code{species_resolved}
is \code{NA} (i.e., species that could not be reconciled) are removed from
the final result. Default \code{FALSE} (keep all rows, fill unmatched
columns with \code{NA}). Only relevant for \code{how = "left"} or \code{how = "full"};
inner joins drop unmatched rows by definition.}
}
\value{
A data frame with a \code{species_resolved} column as the join
key, plus all columns from both datasets (with suffixes added when
column names collide).
}
\description{
After reconciling two datasets with \code{\link[=reconcile_data]{reconcile_data()}}, use this function
to join them into a single analysis-ready data frame. The reconciliation
mapping table provides the species-level join key, so names that differ
between the two datasets (due to formatting, synonyms, or typos) are
correctly linked.
}
\details{
\strong{One row per species.} \code{reconcile_merge()} works best when each dataset
has exactly one row per species. If a species appears in multiple rows
(e.g., sex-specific measurements, repeated populations), the merge
produces all pairwise combinations for that species---the same behaviour
as base \code{\link[=merge]{merge()}}. To avoid unexpected row expansion, aggregate to one
row per species before merging, or be aware that the output will contain
more rows than either input.

\strong{Asymmetric datasets.} When \code{data_y} contains many more species than
\code{data_x} (common when merging against a large reference database), use
\code{how = "inner"} or \code{how = "left"}. Inner joins keep only the species
present in both datasets; left joins keep all \code{data_x} rows and fill
\code{data_y} columns with \code{NA} for unmatched species. Use \code{how = "full"}
only when you need to retain species unique to either side.

\strong{Recommended workflow for multi-row data.} Reconcile using a
species-level summary (one row per species), inspect the mapping with
\code{\link[=reconcile_mapping]{reconcile_mapping()}}, then join the mapping back to your full dataset
using the species column as key.
}
\examples{
data(avonet_subset)
data(nesttrait_subset)

rec <- reconcile_data(avonet_subset, nesttrait_subset,
                      x_species = "Species1",
                      y_species = "Scientific_name",
                      authority = NULL, quiet = TRUE)

merged <- reconcile_merge(rec, avonet_subset, nesttrait_subset,
                          species_col_x = "Species1",
                          species_col_y = "Scientific_name")
cat(sprintf("Merged: \%d rows, \%d cols\n", nrow(merged), ncol(merged)))
head(merged[, c("species_resolved", "Family1", "Common_name")])

}
\seealso{
\code{\link[=reconcile_data]{reconcile_data()}} to build the reconciliation;
\code{\link[=reconcile_apply]{reconcile_apply()}} when you want aligned data + tree instead of a
single merged data frame.

Other reconciliation functions:
\code{\link[=reconcile_apply]{reconcile_apply()}},
\code{\link[=reconcile_augment]{reconcile_augment()}},
\code{\link[=reconcile_crosswalk]{reconcile_crosswalk()}},
\code{\link[=reconcile_crosswalk_supplement]{reconcile_crosswalk_supplement()}},
\code{\link[=reconcile_data]{reconcile_data()}},
\code{\link[=reconcile_diff]{reconcile_diff()}},
\code{\link[=reconcile_export]{reconcile_export()}},
\code{\link[=reconcile_mapping]{reconcile_mapping()}},
\code{\link[=reconcile_multi]{reconcile_multi()}},
\code{\link[=reconcile_override]{reconcile_override()}},
\code{\link[=reconcile_override_batch]{reconcile_override_batch()}},
\code{\link[=reconcile_plot]{reconcile_plot()}},
\code{\link[=reconcile_report]{reconcile_report()}},
\code{\link[=reconcile_review]{reconcile_review()}},
\code{\link[=reconcile_splits_lumps]{reconcile_splits_lumps()}},
\code{\link[=reconcile_suggest]{reconcile_suggest()}},
\code{\link[=reconcile_summary]{reconcile_summary()}},
\code{\link[=reconcile_to_trees]{reconcile_to_trees()}},
\code{\link[=reconcile_tree]{reconcile_tree()}},
\code{\link[=reconcile_trees]{reconcile_trees()}}
}
\concept{reconciliation functions}
