% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pr_phylo_cor.R
\name{pr_phylo_cor}
\alias{pr_phylo_cor}
\title{Phylogenetic correlation matrix from a tree}
\usage{
pr_phylo_cor(x, corr = TRUE, ...)
}
\arguments{
\item{x}{A \code{phylo} object, a \code{multiPhylo}, or a
\code{pr_tree_result} (the \verb{$tree} slot is extracted). For
\code{multiPhylo} input, returns a list of correlation matrices.}

\item{corr}{Logical. Pass through to \code{ape::vcv()}. \code{TRUE}
(default) returns a correlation matrix (diagonal = 1); \code{FALSE}
returns the variance-covariance matrix.}

\item{...}{Additional arguments forwarded to \code{ape::vcv()}.}
}
\value{
A square symmetric matrix with row/column names equal to
the tip labels. For \code{multiPhylo} input, a list of such matrices.
}
\description{
Convert a phylogeny into the correlation matrix used as a random-
effect structure in phylogenetic meta-analysis (\code{metafor::rma.mv})
or phylogenetic mixed models (\code{MCMCglmm}, \code{brms}, etc.).
}
\details{
Wraps \code{\link[ape:vcv]{ape::vcv()}} with \code{corr = TRUE}. Designed to slot in after
\code{\link[=pr_get_tree]{pr_get_tree()}} when the goal is meta-analysis, where typically:
\enumerate{
\item Topology comes from Open Tree of Life (\code{source = "rotl"})
because the species span many higher taxa.
\item Polytomies are resolved at random (\code{resolve_polytomies = TRUE}).
\item Branch lengths are computed via Grafen's method
(\code{branch_lengths = "grafen"}) because rotl's edge lengths are
unit-length placeholders.
\item The correlation matrix is computed once and reused as
\code{random = ~1|species}'s \code{R = list(species = phy_cor)} in
\code{metafor::rma.mv()} (or \code{random = ~species} with
\code{cov.formula = ~ phylo} in \code{MCMCglmm}).
}

The correlation matrix has the property that, for a Brownian-motion
model on a tree with branch lengths in time units, two species'
off-diagonal entry equals the time from root to their MRCA divided
by the time from root to tip. So an ultrametric tree always has
diagonal = 1 (every tip is the same distance from the root).

For meta-analysis with rotl topology + Grafen's method, the
resulting matrix is the standard Pagel's lambda = 1 phylogenetic
correlation that \code{metafor::rma.mv()} accepts directly.
}
\examples{
set.seed(1)
tr <- ape::rcoal(5)             # ultrametric, bifurcating
phy_cor <- pr_phylo_cor(tr)
dim(phy_cor)
all(diag(phy_cor) == 1)

\donttest{
  # End-to-end meta-analysis prep
  if (requireNamespace("rotl", quietly = TRUE)) {
    res <- try(pr_get_tree(c("Homo sapiens", "Pan troglodytes",
                             "Mus musculus", "Rattus norvegicus"),
                           source             = "rotl",
                           resolve_polytomies = TRUE,
                           branch_lengths     = "grafen"),
               silent = TRUE)
    if (!inherits(res, "try-error")) {
      phy_cor <- pr_phylo_cor(res)
      # phy_cor can now be supplied to downstream meta-analysis models.
    }
  }
}

}
\references{
Paradis, E., & Schliep, K. (2019). ape 5.0: an environment for
modern phylogenetics and evolutionary analyses in R.
\emph{Bioinformatics}, 35(3), 526--528.
\doi{10.1093/bioinformatics/bty633}

Cinar, O., Nakagawa, S., & Viechtbauer, W. (2022). Phylogenetic
multilevel meta-analysis: a simulation study on the importance of
modelling the phylogeny. \emph{Methods in Ecology and Evolution},
13(2), 383--395. \doi{10.1111/2041-210X.13760}
}
\seealso{
\code{\link[=pr_get_tree]{pr_get_tree()}} (use with \code{branch_lengths = "grafen"}
and \code{resolve_polytomies = TRUE} for the meta-analysis path);
\code{\link[ape:vcv]{ape::vcv()}} for the underlying computation.
}
