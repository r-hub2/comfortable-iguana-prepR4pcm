% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_accessors.R
\name{reconcile_mapping}
\alias{reconcile_mapping}
\title{Extract the per-name mapping table from a reconciliation}
\usage{
reconcile_mapping(reconciliation, include_unused_overrides = FALSE)
}
\arguments{
\item{reconciliation}{A \link{reconciliation} object returned by
\code{\link[=reconcile_tree]{reconcile_tree()}}, \code{\link[=reconcile_data]{reconcile_data()}}, \code{\link[=reconcile_trees]{reconcile_trees()}},
\code{\link[=reconcile_to_trees]{reconcile_to_trees()}}, or \code{\link[=reconcile_multi]{reconcile_multi()}}.}

\item{include_unused_overrides}{Logical. Appends the rejected
override rows to the returned tibble when \code{TRUE}, with
\code{match_type = "override_unused"}, \code{match_score = NA}, and the
\code{notes} column carrying the rejection reason
(\code{name_x_not_in_data}, \code{name_y_not_in_target}, or
\code{already_matched}). Default \code{FALSE} for backward compatibility ---
the bare mapping tibble has the same shape as before.}
}
\value{
A tibble with one row per unique name seen in either source
and the following columns:
\describe{
\item{\code{name_x}}{Statement: this column holds the original name
as it appeared in source \code{x} (your data). \code{NA} for rows that
exist only in source \code{y} (e.g. tree tips not in your data).}
\item{\code{name_y}}{Statement: this column holds the original name
as it appeared in source \code{y} (the reference dataset or tree).
\code{NA} for rows that exist only in source \code{x}.}
\item{\code{name_resolved}}{The accepted/canonical name returned by
the taxonomic authority, when synonym resolution was used.
\code{NA} when \code{authority = NULL} or no synonym was found.}
\item{\code{match_type}}{One of \code{"exact"}, \code{"normalized"},
\code{"synonym"}, \code{"fuzzy"}, \code{"manual"} (set via
\code{\link[=reconcile_override]{reconcile_override()}}), \code{"flagged"} (low-confidence, needs
review), \code{"unresolved"}, or --- when
\code{include_unused_overrides = TRUE} --- \code{"override_unused"}
(override row not applied because of missing names or prior
matches).}
\item{\code{match_score}}{Numeric in [0, 1]. \code{1} for
exact/normalized/synonym/manual matches; a genus-weighted
Levenshtein score for fuzzy matches; \code{NA} for unresolved and
for unused-override rows.}
\item{\code{match_source}}{Where the match came from: \code{"exact"},
\code{"normalisation"}, the taxadb authority code (e.g. \code{"col"}),
\code{"fuzzy"}, or \code{"user_override"}.}
\item{\code{in_x}}{Logical. This column records whether the name was
present in source \code{x}.}
\item{\code{in_y}}{Logical. This column records whether the name was
present in source \code{y}.}
\item{\code{notes}}{Free-text notes, populated e.g. when a name is
flagged for review or when an override carries a user comment.
For \code{match_type = "override_unused"} rows this column carries
the rejection reason.}
}
}
\description{
Returns the mapping tibble inside a \link{reconciliation} object. Use this
when you want to filter matches programmatically (e.g. pull all
unresolved species, all fuzzy matches above a given score, or join
the mapping back to the original data frame).
}
\examples{
data(avonet_subset)
data(tree_jetz)
rec <- reconcile_tree(avonet_subset, tree_jetz,
                      x_species = "Species1", authority = NULL)
mapping <- reconcile_mapping(rec)

# How many species matched?
sum(mapping$in_x & mapping$in_y)

# Which species are in the data but missing from the tree?
head(mapping[mapping$in_x & !mapping$in_y, c("name_x", "match_type")])

# Append rejected overrides for audit
mapping_full <- reconcile_mapping(rec, include_unused_overrides = TRUE)

}
\seealso{
\code{\link[=reconcile_summary]{reconcile_summary()}} for a printed breakdown;
\code{\link[=reconcile_suggest]{reconcile_suggest()}} for near-miss candidates for unresolved
names; \code{\link[=reconcile_apply]{reconcile_apply()}} to turn the mapping into an aligned
data-tree pair. The unused-override rows surfaced by
\code{include_unused_overrides = TRUE} mirror the \code{unused_overrides}
slot on the \link{reconciliation} object.

Other reconciliation functions:
\code{\link[=reconcile_apply]{reconcile_apply()}},
\code{\link[=reconcile_augment]{reconcile_augment()}},
\code{\link[=reconcile_crosswalk]{reconcile_crosswalk()}},
\code{\link[=reconcile_crosswalk_supplement]{reconcile_crosswalk_supplement()}},
\code{\link[=reconcile_data]{reconcile_data()}},
\code{\link[=reconcile_diff]{reconcile_diff()}},
\code{\link[=reconcile_export]{reconcile_export()}},
\code{\link[=reconcile_merge]{reconcile_merge()}},
\code{\link[=reconcile_multi]{reconcile_multi()}},
\code{\link[=reconcile_override]{reconcile_override()}},
\code{\link[=reconcile_override_batch]{reconcile_override_batch()}},
\code{\link[=reconcile_plot]{reconcile_plot()}},
\code{\link[=reconcile_report]{reconcile_report()}},
\code{\link[=reconcile_review]{reconcile_review()}},
\code{\link[=reconcile_splits_lumps]{reconcile_splits_lumps()}},
\code{\link[=reconcile_suggest]{reconcile_suggest()}},
\code{\link[=reconcile_summary]{reconcile_summary()}},
\code{\link[=reconcile_to_trees]{reconcile_to_trees()}},
\code{\link[=reconcile_tree]{reconcile_tree()}},
\code{\link[=reconcile_trees]{reconcile_trees()}}
}
\concept{reconciliation functions}
