% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_accessors.R
\name{reconcile_apply}
\alias{reconcile_apply}
\title{Apply a reconciliation to produce an aligned data-tree pair}
\usage{
reconcile_apply(
  reconciliation,
  data = NULL,
  tree = NULL,
  species_col = NULL,
  drop_unresolved = FALSE
)
}
\arguments{
\item{reconciliation}{A \link{reconciliation} object returned by
\code{\link[=reconcile_tree]{reconcile_tree()}}, \code{\link[=reconcile_data]{reconcile_data()}}, or a related matcher.}

\item{data}{A data frame to align. If \code{NULL}, only the tree is
processed and the returned \code{data} slot is \code{NULL}.}

\item{tree}{An \code{ape::phylo} object (or path to a Newick / Nexus
file) to align. If \code{NULL}, only the data frame is processed and
the returned \code{tree} slot is \code{NULL}. (Passing both \code{data = NULL}
and \code{tree = NULL} is allowed but produces an empty result; the
normal use is to pass at least one of them.)}

\item{species_col}{A length-1 character vector. Column in \code{data}
containing species names. Auto-detected from a small set of
common heuristics (e.g. \code{species}, \code{Species1}, \code{scientific_name})
when \code{NULL}; the heuristics list is not exhaustive --- pass the
column name explicitly if your data uses a non-standard label.}

\item{drop_unresolved}{Logical. Drops unmatched rows and tips when \code{TRUE}.
Defaults to \code{FALSE} (keep everything and just warn). Set to \code{TRUE}
when preparing data for an analysis that cannot tolerate mismatches.}
}
\value{
A list with two elements:
\describe{
\item{\code{data}}{The aligned data frame (or \code{NULL} if \code{data} was
not supplied).}
\item{\code{tree}}{The aligned \code{phylo} object (or \code{NULL} if \code{tree}
was not supplied).}
}
}
\description{
Turn a \link{reconciliation} object into an analysis-ready data frame and
pruned phylogenetic tree whose species labels agree. This is the step
that feeds directly into \code{\link[caper:pgls]{caper::pgls()}}, \code{\link[MCMCglmm:MCMCglmm]{MCMCglmm::MCMCglmm()}},
\code{\link[phytools:fastAnc]{phytools::fastAnc()}}, or any other PCM that expects matching names
in data and tree.
}
\details{
Rows in \code{data} whose species have no match in the tree (and tips in
\code{tree} whose species have no match in the data) are handled according
to \code{drop_unresolved}. Matched data rows are kept as-is. Matched tree
tips are renamed to the source-\code{x} (data-side) name when the tree-side
label differs, so downstream PCM software can look up tips by the
species names in your data frame.
}
\examples{
data(avonet_subset)
data(tree_jetz)
rec <- reconcile_tree(avonet_subset, tree_jetz,
                      x_species = "Species1", authority = NULL)

aligned <- reconcile_apply(rec,
                           data = avonet_subset,
                           tree = tree_jetz,
                           species_col = "Species1",
                           drop_unresolved = TRUE)
nrow(aligned$data)
ape::Ntip(aligned$tree)

# aligned$data and aligned$tree are ready for downstream PCM tools

}
\seealso{
\code{\link[=reconcile_tree]{reconcile_tree()}} to build the reconciliation;
\code{\link[=reconcile_merge]{reconcile_merge()}} when you want a single merged data frame
instead of aligned data + tree; \code{\link[=reconcile_export]{reconcile_export()}} to write
everything to disk.

Other reconciliation functions:
\code{\link[=reconcile_augment]{reconcile_augment()}},
\code{\link[=reconcile_crosswalk]{reconcile_crosswalk()}},
\code{\link[=reconcile_crosswalk_supplement]{reconcile_crosswalk_supplement()}},
\code{\link[=reconcile_data]{reconcile_data()}},
\code{\link[=reconcile_diff]{reconcile_diff()}},
\code{\link[=reconcile_export]{reconcile_export()}},
\code{\link[=reconcile_mapping]{reconcile_mapping()}},
\code{\link[=reconcile_merge]{reconcile_merge()}},
\code{\link[=reconcile_multi]{reconcile_multi()}},
\code{\link[=reconcile_override]{reconcile_override()}},
\code{\link[=reconcile_override_batch]{reconcile_override_batch()}},
\code{\link[=reconcile_plot]{reconcile_plot()}},
\code{\link[=reconcile_report]{reconcile_report()}},
\code{\link[=reconcile_review]{reconcile_review()}},
\code{\link[=reconcile_splits_lumps]{reconcile_splits_lumps()}},
\code{\link[=reconcile_suggest]{reconcile_suggest()}},
\code{\link[=reconcile_summary]{reconcile_summary()}},
\code{\link[=reconcile_to_trees]{reconcile_to_trees()}},
\code{\link[=reconcile_tree]{reconcile_tree()}},
\code{\link[=reconcile_trees]{reconcile_trees()}}
}
\concept{reconciliation functions}
