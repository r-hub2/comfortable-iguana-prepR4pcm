% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconciliation-class.R
\name{reconciliation}
\alias{reconciliation}
\alias{new_reconciliation}
\title{The \code{reconciliation} S3 class}
\usage{
new_reconciliation(
  mapping,
  meta,
  counts = NULL,
  overrides = NULL,
  unused_overrides = NULL
)
}
\arguments{
\item{mapping}{A tibble with the mapping table (see above).}

\item{meta}{A named list of provenance metadata.}

\item{counts}{A named list of summary counts. Computed from
\code{mapping} if \code{NULL}.}

\item{overrides}{A tibble of manual overrides (empty by default).}

\item{unused_overrides}{A tibble of overrides that could not be
applied, with columns \code{name_x}, \code{name_y}, \code{reason}. If \code{NULL},
pulled from \code{attr(mapping, "unused_overrides")} when present,
else initialised empty.}
}
\value{
An object of class \code{reconciliation}.
}
\description{
A \code{reconciliation} object is the shared data structure that every
matching function in \pkg{prepR4pcm} returns, and that every
downstream function consumes. You will never build one by hand;
call \code{\link[=reconcile_tree]{reconcile_tree()}}, \code{\link[=reconcile_data]{reconcile_data()}}, \code{\link[=reconcile_trees]{reconcile_trees()}},
\code{\link[=reconcile_to_trees]{reconcile_to_trees()}}, or \code{\link[=reconcile_multi]{reconcile_multi()}} instead. This page
documents the structure so you can poke at the internals when
debugging or writing custom helpers.
}
\section{Structure}{

A \code{reconciliation} is an S3 list with five components:
\describe{
\item{\code{mapping}}{A tibble with one row per unique name seen in
either source. Columns are documented in \code{\link[=reconcile_mapping]{reconcile_mapping()}}:
\code{name_x}, \code{name_y}, \code{name_resolved}, \code{match_type} (one of
\code{"exact"}, \code{"normalized"}, \code{"synonym"}, \code{"fuzzy"}, \code{"manual"},
\code{"flagged"}, \code{"unresolved"}, or --- when surfaced via
\code{reconcile_mapping(include_unused_overrides = TRUE)} ---
\code{"override_unused"}), \code{match_score}, \code{match_source}, \code{in_x},
\code{in_y}, \code{notes}.}
\item{\code{meta}}{A named list of provenance metadata --- call
signature, timestamp, source labels, taxonomic authority,
fuzzy settings, resolve mode, rank, \pkg{prepR4pcm} version.}
\item{\code{counts}}{A named list of match-type counts, used by the
print method and by \code{\link[=reconcile_summary]{reconcile_summary()}}.}
\item{\code{overrides}}{A tibble logging manual corrections applied
via \code{\link[=reconcile_override]{reconcile_override()}} or \code{\link[=reconcile_override_batch]{reconcile_override_batch()}}.}
\item{\code{unused_overrides}}{A tibble of overrides that the cascade
could NOT apply, with columns \code{name_x}, \code{name_y}, and \code{reason}
(one of \code{name_x_not_in_data}, \code{name_y_not_in_target}, or
\code{already_matched}). Empty when no overrides were supplied or
when every override applied successfully. Surfaced in
\code{reconcile_summary()}, \code{reconcile_report()} (HTML),
\code{reconcile_export()} (as \verb{<prefix>_unused_overrides.csv}), and
\code{reconcile_mapping(include_unused_overrides = TRUE)}.}
}
}

\section{Methods}{

Standard S3 methods are defined for \code{print()}, \code{summary()} (which
dispatches to \code{\link[=reconcile_summary]{reconcile_summary()}}), and \code{format()}.
}

\section{Accessing the object}{

\itemize{
\item \code{\link[=reconcile_mapping]{reconcile_mapping()}} --- extract the per-name tibble.
\item \code{\link[=reconcile_summary]{reconcile_summary()}} --- human-readable breakdown.
\item \code{\link[=reconcile_apply]{reconcile_apply()}} --- align data and tree.
\item \code{\link[=reconcile_merge]{reconcile_merge()}} --- join two datasets.
\item \code{\link[=reconcile_override]{reconcile_override()}} / \code{\link[=reconcile_override_batch]{reconcile_override_batch()}} ---
manual corrections.
}
}

\keyword{internal}
