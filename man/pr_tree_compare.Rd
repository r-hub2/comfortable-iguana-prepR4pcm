% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pr_tree_compare.R
\name{pr_tree_compare}
\alias{pr_tree_compare}
\title{Compare two or more phylogenetic trees}
\usage{
pr_tree_compare(..., prune_to_common = TRUE)
}
\arguments{
\item{...}{Two or more \code{phylo} objects, or two or more
\code{pr_tree_result} objects (the \code{tree} slot is extracted), or
\code{multiPhylo} objects (the first tree is used). Trees can be
passed as positional arguments or as a named list.}

\item{prune_to_common}{Logical. Restrict each tree to the shared
tip set before computing topology metrics? Default \code{TRUE} ---
without this, RF distance is undefined when tip sets differ.}
}
\value{
A list with class \code{pr_tree_compare} and components:
\describe{
\item{\code{n_trees}}{Number of input trees.}
\item{\code{tip_sets}}{Named list of character vectors, one per tree.}
\item{\code{shared_tips}}{Tips present in every input tree.}
\item{\code{unique_to}}{Named list, one per tree, of tips present in
that tree but not in every other tree.}
\item{\code{n_shared}}{Length-1 integer.}
\item{\code{pairwise_jaccard}}{Square matrix; \verb{(i, j)} is the Jaccard
index of \verb{tip_sets[[i]] vs tip_sets[[j]]}.}
\item{\code{pairwise_rf}}{Square matrix of Robinson-Foulds distances
between pairs of trees pruned to \code{shared_tips}. \code{NA} when the
pair has < 4 shared tips.}
\item{\code{pairwise_branch_cor}}{Square matrix of Pearson
correlations between matching edge lengths in each pair, or
\code{NA} when one or both trees have no branch lengths.}
}
}
\description{
Computes a small set of standard metrics for comparing trees that
come from different backends (or different runs of the same
backend). Designed for the common case of "I retrieved a tree from
rotl and another from fishtree --- do they agree?"
}
\details{
RF distance is computed via \code{\link[ape:dist.topo]{ape::dist.topo()}} with the default
method. Branch-length correlation matches edges by their tip-set
bipartition: for each edge in tree A, the corresponding edge in
tree B (if any) is the one that splits the same set of tips. The
Pearson correlation is taken over the matched edge-length pairs;
edges whose bipartition is absent in the other tree are dropped.
This is a proper bipartition-matched correlation as introduced in
Kuhner & Felsenstein (1994) for tree comparison.
}
\examples{
# Two trees with identical tip sets
set.seed(1)
t1 <- ape::rtree(10)
t2 <- ape::rtree(10, tip.label = t1$tip.label)
cmp <- pr_tree_compare(t1, t2)
cmp$n_shared
cmp$pairwise_rf

# Two trees with overlapping but not identical tips
t3 <- ape::rtree(8, tip.label = t1$tip.label[1:8])
cmp <- pr_tree_compare(t1, t3)
cmp$pairwise_jaccard

}
\references{
Kuhner, M. K., & Felsenstein, J. (1994). A simulation comparison
of phylogeny algorithms under equal and unequal evolutionary rates.
\emph{Molecular Biology and Evolution} 11(3): 459--468.
\doi{10.1093/oxfordjournals.molbev.a040126}

Robinson, D. F., & Foulds, L. R. (1981). Comparison of phylogenetic
trees. \emph{Mathematical Biosciences} 53(1--2): 131--147.
\doi{10.1016/0025-5564(81)90043-2}
}
\seealso{
\code{\link[=pr_get_tree]{pr_get_tree()}} for retrieval; \code{\link[=reconcile_apply]{reconcile_apply()}} for
combining a chosen tree with a dataset.
}
