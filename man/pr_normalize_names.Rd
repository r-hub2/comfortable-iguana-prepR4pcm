% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pr_normalize.R
\name{pr_normalize_names}
\alias{pr_normalize_names}
\title{Normalise scientific names to a canonical form}
\usage{
pr_normalize_names(
  names,
  rank = c("species", "subspecies"),
  parser = c("internal", "gnparser")
)
}
\arguments{
\item{names}{A character vector of scientific names (any length;
each element is a single name). \code{NA} values are preserved as
\code{NA}.}

\item{rank}{A length-1 character vector. Taxonomic rank to normalise to:
\describe{
\item{\code{"species"} (default)}{Strip infraspecific epithets so
trinomials become binomials (\verb{Parus major major} ->
\verb{Parus major}).}
\item{\code{"subspecies"}}{Keep trinomials intact.}
}}

\item{parser}{A length-1 character vector. Which parsing engine to
use:
\describe{
\item{\code{"internal"} (default)}{The package's own regex-based
cascade described above. No external dependency.}
\item{\code{"gnparser"}}{Delegates parsing to
\code{\link[rgnparser:gn_parse_tidy]{rgnparser::gn_parse_tidy()}}, which wraps the
\href{https://github.com/gnames/gnparser}{gnparser} Go binary
(part of the Global Names Architecture). Handles hybrid
signs, complex multi-author year strings, and trailing
parentheticals (Open Tree homonym / rank flags) more
robustly than the internal cascade. Requires both the
\pkg{rgnparser} R package and the \code{gnparser} binary on the
system PATH; the function errors helpfully if either is
missing. Returns the same shape and \code{normalisation_log}
attribute as the internal path, so the two are drop-in
interchangeable.}
}}
}
\value{
A character vector of normalised names, the same length as
\code{names}, with an attribute \code{"normalisation_log"} --- a tibble
recording every non-trivial change, for auditing.
}
\description{
Apply a sequence of deterministic text transformations so that
scientific names which differ only in formatting compare equal.
This is the same routine used by stage 2 of the matching cascade in
\code{\link[=reconcile_data]{reconcile_data()}} and \code{\link[=reconcile_tree]{reconcile_tree()}}. Use it directly when you
want to clean a column of names without running a full
reconciliation --- for example, when building a crosswalk by hand.
}
\details{
The transformations, applied in order, are:
\enumerate{
\item Replace underscores and multiple whitespace with a single
space (\code{Homo_sapiens} -> \verb{Homo sapiens}).
\item Strip authority strings and year, including multi-author
and parenthetical forms (\verb{Corvus corax (Linnaeus, 1758)} ->
\verb{Corvus corax}).
\item Strip any other trailing parenthetical qualifier, such as
the Open Tree of Life homonym / rank flags that \code{rotl} returns
(\verb{Prunella (genus in kingdom Archaeplastida)} -> \code{Prunella}).
\item Fold diacritics to ASCII (\verb{Passer domesticus} stays as
\verb{Passer domesticus}; accented characters are simplified).
\item Standardise case: genus capitalised, epithet lowercase.
\item Strip infraspecific epithets if \code{rank = "species"}.
\item Trim whitespace and collapse leftover empty tokens.
}
}
\note{
On the spelling: the title and prose use British English
\emph{normalise}, consistent with the package's
\code{Language: en-GB} declaration. The function identifier
\code{pr_normalize_names()} keeps the American-English \code{z} because
R-package function names conventionally use ASCII identifiers
in the form most R users expect. The two spellings are
equivalent and intentional.
}
\examples{
pr_normalize_names(c("Homo_sapiens",
                     "homo sapiens",
                     "Parus major major",
                     "Corvus corax (Linnaeus, 1758)"))

# Keep trinomials
pr_normalize_names("Parus major major", rank = "subspecies")

}
\seealso{
\code{\link[=reconcile_data]{reconcile_data()}} and \code{\link[=reconcile_tree]{reconcile_tree()}} for the full
four-stage matching cascade; \code{\link[=pr_extract_tips]{pr_extract_tips()}} for pulling tip
labels out of a tree prior to normalising them.

Other name utilities:
\code{\link[=pr_extract_tips]{pr_extract_tips()}}
}
\concept{name utilities}
