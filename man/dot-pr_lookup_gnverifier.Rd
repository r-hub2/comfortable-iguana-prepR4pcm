% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pr_authority.R
\name{.pr_lookup_gnverifier}
\alias{.pr_lookup_gnverifier}
\title{Look up names via the Global Names verifier (HTTP)}
\usage{
.pr_lookup_gnverifier(names, db_version = NULL)
}
\arguments{
\item{names}{Character vector of names to verify.}

\item{db_version}{Ignored; emits a single warning if non-NULL.}
}
\value{
A tibble with the same 5 columns as \code{pr_lookup_authority()}.
}
\description{
Internal helper for \code{pr_lookup_authority(authority = "gnverifier")}.
POSTs the input vector to the
\href{https://verifier.globalnames.org/}{Global Names verifier} and maps
each \code{bestResult} back to the 5-column tibble contract used by the
taxadb path. Returns all-rows-\code{not_found} and emits a single
warning on network failure, mirroring the taxadb branch's
degradation behaviour so the cascade above keeps running.
}
\keyword{internal}
