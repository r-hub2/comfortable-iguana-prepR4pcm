% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_crosswalk_supplement.R
\name{reconcile_crosswalk_supplement}
\alias{reconcile_crosswalk_supplement}
\title{Apply a taxonomy crosswalk as a post-baseline supplement}
\usage{
reconcile_crosswalk_supplement(
  reconciliation,
  crosswalk,
  from_col,
  to_col,
  match_type_col = NULL,
  notes_col = NULL,
  one_to_one_only = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{reconciliation}{A \link{reconciliation} object returned by
\code{\link[=reconcile_data]{reconcile_data()}}, \code{\link[=reconcile_tree]{reconcile_tree()}}, or a related matcher.}

\item{crosswalk}{A data frame, or a file path. File format is
inferred from the extension: \code{.csv} (comma-separated), \code{.tsv}
(tab-separated), or \code{.txt} (tab-separated). For other delimited
formats, read the file yourself with \code{read.delim()} or
\code{read.table()} and pass the resulting data frame.}

\item{from_col}{A length-1 character vector. Column name for source names (e.g.,
\code{"Species1"} for BirdLife names).}

\item{to_col}{A length-1 character vector. Column name for target names (e.g.,
\code{"Species3"} for BirdTree names).}

\item{match_type_col}{A length-1 character vector or \code{NULL}. Name
of an optional column in \code{crosswalk} that classifies each row's
relationship between the two taxonomies --- e.g. \code{"1BL to 1BT"}
(one BirdLife species mapped to one BirdTree species; a clean
one-to-one match), \code{"Many BL to 1BT"} (a lump: several BirdLife
species mapped to a single BirdTree species), \code{"1BL to many BT"}
(a split). When supplied, the contents of this column are
appended to each override's \code{user_note} so the audit trail
records the relationship; if you also pass
\code{one_to_one_only = TRUE}, only the rows whose match type starts
\code{"1...to 1..."} are kept. Pass \code{NULL} (default) when your
crosswalk has no such classification column --- every row is
then kept and notes carry no provenance label.}

\item{notes_col}{A length-1 character vector or NULL. Column containing additional
notes.}

\item{one_to_one_only}{Logical. If \code{TRUE} (default), keeps only
one-to-one crosswalk rows before supplementing the baseline result.}

\item{quiet}{Logical. Suppresses informational messages when \code{TRUE}.}
}
\value{
An updated \link{reconciliation} object. If no unambiguous crosswalk
rows can supplement the baseline result, the returned object is the
input \code{reconciliation} with a \code{meta$crosswalk_supplement} audit entry.
}
\description{
Use this after an initial \code{\link[=reconcile_data]{reconcile_data()}} or \code{\link[=reconcile_tree]{reconcile_tree()}} run
when you want a taxonomy crosswalk to add matches without overwriting
exact, normalised, synonym, or fuzzy matches that the baseline cascade
already made.
}
\details{
Passing the output of \code{\link[=reconcile_crosswalk]{reconcile_crosswalk()}} directly to the
\code{overrides} argument of a \verb{reconcile_*()} call treats every row as a
locked manual decision. Those overrides are applied before the matching
cascade, so they can preempt exact or normalised matches. That is correct
for reviewed manual corrections, but risky when using a whole published
crosswalk automatically.

\code{reconcile_crosswalk_supplement()} implements the safer pattern:

\enumerate{
\item run the baseline reconciliation first;
\item convert the crosswalk to candidate overrides;
\item keep only rows whose source name is still unresolved in \code{x} and
whose target name is still unresolved in \code{y};
\item drop duplicate source or target candidates rather than choosing by
row order;
\item apply the remaining rows with \code{\link[=reconcile_override_batch]{reconcile_override_batch()}}.
}

By default, \code{one_to_one_only = TRUE}, so split/lump rows such as
\code{"1BL to many BT"} and \code{"Many BL to 1BT"} are not applied
automatically. If you set \code{one_to_one_only = FALSE}, duplicate source or
target candidates are still skipped and should be reviewed manually.
}
\examples{
x <- data.frame(species = c("Species old", "Species exact"))
y <- data.frame(species = c("Species new", "Species exact"))
crosswalk <- data.frame(
  from = "Species old",
  to = "Species new",
  type = "1BL to 1BT"
)

baseline <- reconcile_data(
  x, y,
  x_species = "species",
  y_species = "species",
  authority = NULL,
  quiet = TRUE
)

supplemented <- reconcile_crosswalk_supplement(
  baseline,
  crosswalk,
  from_col = "from",
  to_col = "to",
  match_type_col = "type",
  quiet = TRUE
)
reconcile_mapping(supplemented)

}
\seealso{
\code{\link[=reconcile_crosswalk]{reconcile_crosswalk()}} for converting a crosswalk to an override
table; \code{\link[=reconcile_override_batch]{reconcile_override_batch()}} for applying reviewed batches.

Other reconciliation functions:
\code{\link[=reconcile_apply]{reconcile_apply()}},
\code{\link[=reconcile_augment]{reconcile_augment()}},
\code{\link[=reconcile_crosswalk]{reconcile_crosswalk()}},
\code{\link[=reconcile_data]{reconcile_data()}},
\code{\link[=reconcile_diff]{reconcile_diff()}},
\code{\link[=reconcile_export]{reconcile_export()}},
\code{\link[=reconcile_mapping]{reconcile_mapping()}},
\code{\link[=reconcile_merge]{reconcile_merge()}},
\code{\link[=reconcile_multi]{reconcile_multi()}},
\code{\link[=reconcile_override]{reconcile_override()}},
\code{\link[=reconcile_override_batch]{reconcile_override_batch()}},
\code{\link[=reconcile_plot]{reconcile_plot()}},
\code{\link[=reconcile_report]{reconcile_report()}},
\code{\link[=reconcile_review]{reconcile_review()}},
\code{\link[=reconcile_splits_lumps]{reconcile_splits_lumps()}},
\code{\link[=reconcile_suggest]{reconcile_suggest()}},
\code{\link[=reconcile_summary]{reconcile_summary()}},
\code{\link[=reconcile_to_trees]{reconcile_to_trees()}},
\code{\link[=reconcile_tree]{reconcile_tree()}},
\code{\link[=reconcile_trees]{reconcile_trees()}}
}
\concept{reconciliation functions}
