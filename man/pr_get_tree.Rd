% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pr_get_tree.R
\name{pr_get_tree}
\alias{pr_get_tree}
\title{Retrieve a candidate phylogeny for a species list}
\usage{
pr_get_tree(
  x,
  source = c("rotl", "rtrees", "clootl", "fishtree", "datelife", "auto"),
  species_col = NULL,
  taxon = NULL,
  n_tree = 1L,
  cache = FALSE,
  tnrs = c("auto", "always", "never"),
  min_match = 0.8,
  check_ultrametric = TRUE,
  resolve_polytomies = FALSE,
  branch_lengths = NULL,
  ...
)
}
\arguments{
\item{x}{One of:
\describe{
\item{a \code{reconciliation} object}{returned by \code{\link[=reconcile_tree]{reconcile_tree()}}
or \code{\link[=reconcile_data]{reconcile_data()}}; species are taken from the reconciled
\code{name_y} column with \code{NA}s and unresolved entries dropped.}
\item{a character vector}{used directly after deduplication and
NA removal.}
\item{a data frame}{\code{species_col} must name a character column;
its unique non-NA values are used.}
}}

\item{source}{A length-1 character vector. Which external backend
to use. One of:
\describe{
\item{\code{"rotl"}}{Open Tree of Life synthesis tree, via the
CRAN package \code{rotl}. Universal taxonomic coverage; calls
\code{tnrs_match_names()} to resolve names to OTT ids and
then \code{tol_induced_subtree()}.}
\item{\code{"rtrees"}}{Taxon-specific mega-trees (bird, mammal,
fish, amphibian, reptile, plant, shark/ray, bee, butterfly)
via the CRAN package \code{rtrees}
(\url{https://daijiang.github.io/rtrees/}). Requires
\code{taxon = "<group>"}. Calls \code{get_tree()}. Install
with \code{install.packages("rtrees")}. The project website
links to upstream documentation.
\strong{Grafting behaviour:} when an input species is not in
the chosen mega-tree, \code{rtrees::get_tree()} grafts it at
the genus level (tip suffix \verb{*}) or family level
(\verb{**}); if no co-family species is in the mega-tree, the
species is dropped. The placement of every input species is
reported per-row in \code{result$backend_meta$placement} (a
tibble with columns \code{input_name}, \code{tree_name},
\code{placement_status} where \code{placement_status} is one
of \code{"exact"}, \code{"genus_added"}, \code{"family_added"},
\code{"skipped"}, or \code{"unmatched"}). The grafting itself
cannot be disabled at the wrapper level:
\code{rtrees::get_tree()} has no exact-only switch. To exclude
grafted tips from a downstream analysis,
filter the placement table on \code{placement_status == "exact"}
and prune the tree to those tip labels. See
\code{?rtrees::get_tree} for upstream control (\code{scenario}
\emph{where} a graft is placed, but not \emph{whether}).}
\item{\code{"clootl"}}{Bird-only phylogenies in current
Clements taxonomy, via the CRAN package \code{clootl}
(\url{https://github.com/eliotmiller/clootl}). Calls
\code{extractTree()}. Install with
\code{install.packages("clootl")}.}
\item{\code{"fishtree"}}{Fish-only time-calibrated phylogeny
(Rabosky et al. 2018), via the CRAN package \code{fishtree}.
Calls \code{fishtree_phylogeny()} (single tree) or
\code{fishtree_complete_phylogeny()} (multi-tree posterior;
triggered by \code{n_tree > 1}). Requires exact name
matches against the Fish Tree of Life taxonomy --- pre-clean
with \code{\link[=reconcile_data]{reconcile_data()}} (with a \code{taxadb} authority) for best
results.}
\item{\code{"datelife"}}{Universal database of pre-computed
chronograms (Sanchez Reyes et al. 2024, \emph{Syst. Biol.}
73:470), via the GitHub package \code{datelife}
(\url{https://github.com/phylotastic/datelife}). Returns a
single SDM-summary chronogram by default; with
\code{n_tree > 1}, returns a multiPhylo of up to that many
per-source candidate chronograms. \strong{Install before use}
with \code{pak::pak("phylotastic/datelife")} --- the package
is GitHub-only (archived from CRAN in 2024 with a heavy
transitive dep tree pak can't auto-resolve), so \pkg{prepR4pcm}
does NOT pull it in via \code{Suggests}.}
\item{\code{"auto"}}{Fall-through dispatcher: try installed
backends in priority order (rtrees if \code{taxon} provided,
then rotl, fishtree, clootl, datelife), return the first
result that resolves at least \code{min_match} of the
species. Useful for first-pass exploration when you don't
yet know which backend covers your taxa.}
}}

\item{species_col}{A length-1 character vector. Required when \code{x}
is a data frame; ignored otherwise.}

\item{taxon}{A length-1 character vector. Required when
\code{source = "rtrees"}. One of \code{"bird"}, \code{"mammal"}, \code{"fish"},
\code{"amphibian"}, \code{"reptile"}, \code{"plant"}, \code{"shark_ray"}, \code{"bee"},
\code{"butterfly"} (see the \code{rtrees} package help for
\code{get_tree}). Ignored for other backends.}

\item{n_tree}{A length-1 positive integer. How many trees to
request from the backend. Default \code{1L} (single phylo for
back-compat). Each backend negotiates this differently:
\describe{
\item{\code{"rotl"}}{Always returns 1 (the synthesis tree). A
one-shot warning is emitted if \code{n_tree > 1}.}
\item{\code{"rtrees"}}{\strong{\code{n_tree} is informational only here.}
\code{rtrees::get_tree()} does not have an \code{n_tree} argument;
the multi-tree count is fixed by which mega-tree was
selected. Reference trees rtrees uses internally:
birds = Jetz et al. 2012
(\url{https://birdtree.org}, 100 posterior trees);
mammals = Upham et al. 2019 (VertLife, 100 by default;
set \code{mammal_tree = "phylacine"} for the PHYLACINE set);
amphibians + squamates = VertLife; fish = Rabosky et al. 2018
(also wrapped by \code{source = "fishtree"}); plants =
V.PhyloMaker; bees = Bee Tree of Life. Override which
mega-tree is used via \code{...} (e.g. \code{bee_tree = "bootstrap"}
for 100 bee trees instead of the single ML tree).
Requires \code{taxon}.}
\item{\code{"clootl"}}{\code{n_tree = 1} calls \code{clootl::extractTree()}
and works out of the box with the v1.6 / 2025 taxonomy
bundled in the \code{clootl} package. \code{n_tree > 1} calls
\code{clootl::sampleTrees(count = n_tree)} (capped at 100
upstream) and \strong{requires the AvesData repo to be set up
once via \code{clootl::get_avesdata_repo(".")} first};
otherwise it errors with \verb{AvesData repo not found}.}
\item{\code{"fishtree"}}{Single phylo via \code{fishtree_phylogeny()}
when \code{n_tree = 1}; switches to
\code{fishtree_complete_phylogeny()} returning a multiPhylo of
stochastically polytomy-resolved trees when \code{n_tree > 1}.}
\item{\code{"datelife"}}{\code{summary_format = "phylo_sdm"} (single
summary chronogram) when \code{n_tree = 1}; switches to
\code{summary_format = "phylo_all"} (one chronogram per source,
capped at \code{n_tree}) when \code{n_tree > 1}.}
}
When the request returns a multiPhylo, the result's \code{tree} slot
is \code{multiPhylo}; otherwise \code{phylo}.}

\item{cache}{Logical. Cache the result on disk and reuse it on
subsequent identical calls? Default \code{FALSE}. When \code{TRUE}, the
request is keyed by \verb{(species, source, n_tree, taxon, tnrs, ...)} and
stored at \code{\link[=pr_tree_cache_dir]{pr_tree_cache_dir()}}. See \code{\link[=pr_tree_cache_status]{pr_tree_cache_status()}}
and \code{\link[=pr_tree_cache_clear]{pr_tree_cache_clear()}} for inspecting / wiping the cache.}

\item{tnrs}{A length-1 character vector. Run a TNRS preflight
(Open Tree of Life name resolution via \code{rotl::tnrs_match_names})
on the species list before calling the backend? One of:
\describe{
\item{\code{"auto"} (default)}{Run TNRS only for \code{fishtree}, where
OTL-resolved names tend to improve the match rate. \strong{Not
run for \code{clootl} by default}: clootl uses the eBird /
Clements taxonomy, so OTL-resolved names are often
different from clootl's preferred names; the network call
is also the dominant cost for large requests (~15 min for
10k species before this change). Pass \code{tnrs = "always"} if
you want it for clootl anyway.}
\item{\code{"always"}}{Run TNRS regardless of backend.}
\item{\code{"never"}}{Skip TNRS even when the backend would benefit.}
}
When \code{rotl} is not installed, TNRS is silently skipped with a
one-shot warning.}

\item{min_match}{A length-1 numeric in \verb{[0, 1]}. Only used when
\code{source = "auto"}. The minimum fraction of input species a
backend must resolve for the dispatcher to accept its result;
if no backend meets the threshold, the best available is
returned with a warning. Default \code{0.8}.}

\item{check_ultrametric}{Logical. After producing the tree, check
that it's ultrametric (all tips equidistant from the root) and
warn if not. Default \code{TRUE}. Only enforced for backends that
normally return chronograms (\code{rtrees}, \code{clootl}, \code{fishtree},
\code{datelife}); \code{rotl} returns a topology without real branch
lengths, so the check is skipped. To force ultrametricity on a
non-ultrametric result, use \code{phytools::force.ultrametric()} or
\code{ape::chronos()} directly --- prepR4pcm does not modify the
tree silently.}

\item{resolve_polytomies}{Logical. After retrieval, resolve
any polytomies via \code{\link[ape:multi2di]{ape::multi2di()}} with \code{random = TRUE}?
Default \code{FALSE} (back-compat; topology preserved). Useful for
phylogenetic meta-analysis, where a strictly bifurcating tree
is required for \code{\link[=pr_phylo_cor]{pr_phylo_cor()}} / \code{\link[ape:vcv]{ape::vcv()}} to produce a
full-rank correlation matrix.}

\item{branch_lengths}{A length-1 character vector or \code{NULL}. After
retrieval (and after polytomy resolution if requested), assign
branch lengths via the named method? Default \code{NULL} (no
transformation; backend's branch lengths are kept as-is). Other
values:
\describe{
\item{\code{"grafen"}}{Grafen's (1989) method via
\code{\link[ape:compute.brlen]{ape::compute.brlen()}} with \code{method = "Grafen"}. The
canonical choice for phylogenetic meta-analysis when the
topology comes from \code{rotl} (whose edge lengths are
unit-length placeholders). See Cinar et al. (2022)
\emph{Methods Ecol. Evol.} 13:383, who use this exact
pattern.}
\item{\code{"compute.brlen"}}{Same as \code{"grafen"} --- Grafen is
\code{ape::compute.brlen()}'s default method. Provided as an
alias for users who think in terms of the underlying
function name.}
\item{\code{"unit"}}{Set every edge length to 1. The crudest
option; useful only for sensitivity-analysis comparisons.}
}}

\item{...}{Backend-specific arguments forwarded to the underlying
call. See the help page of the underlying function in the
relevant backend package (\code{tol_induced_subtree} in
\code{rotl}, \code{extractTree} in \code{clootl},
\code{get_tree} in \code{rtrees}, \code{fishtree_phylogeny} /
\code{fishtree_complete_phylogeny} in \code{fishtree},
\code{datelife_search} in \code{datelife}) for the full list.}
}
\value{
A list with class \code{pr_tree_result} and components:
\describe{
\item{\code{tree}}{A \code{phylo} (single) or \code{multiPhylo} (posterior) object
from the chosen backend, pruned to the matched species.}
\item{\code{matched}}{Character vector of names from the user's
\strong{original input} (preserving the input format, including any
underscores) that resolved to a tip in \code{tree}. The dispatcher
enforces that matched names are a subset of \code{unique(input)} ---
TNRS substitution,
normalisation, and backend-internal name juggling cannot leak
intermediate names into this slot.}
\item{\code{unmatched}}{Character vector of names from the \strong{original
input} that did not resolve. Disjoint from \code{matched};
\code{length(matched) + length(unmatched) == length(unique(input))}
always holds. Inspect these and consider running them back
through \code{\link[=reconcile_suggest]{reconcile_suggest()}} / a manual override.}
\item{\code{mapping}}{A tibble with one row per unique input species.
Core columns: \code{input_name}, \code{normalized_name}, \code{query_name},
\code{tree_name}, \code{in_tree}, \code{match_type}, and \code{placement_status}.
This is the audit trail for name handling: \code{input_name} is what
the user supplied, \code{normalized_name} is the result of
\code{\link[=pr_normalize_names]{pr_normalize_names()}}, \code{query_name} is the backend query after
optional TNRS, \code{tree_name} is the actual returned tip label, and
\code{match_type} is one of \code{"exact"}, \code{"normalized"}, \code{"tnrs"}, or
\code{"unmatched"}. For \code{source = "rtrees"}, \code{placement_status}
carries the grafting status from \code{backend_meta$placement};
otherwise it is \code{NA}. Four further columns record what \code{rotl}'s
TNRS resolver reported for each name: \code{tnrs_number_matches},
\code{tnrs_is_synonym}, \code{tnrs_approximate_match}, and \code{tnrs_flags}.
These are \code{NA} for backends or \code{tnrs} settings where TNRS did
not run. \code{tnrs_number_matches > 1} flags a homonym, meaning the
resolved name is only one of several candidate taxa.}
\item{\code{source}}{The backend that produced the tree.}
\item{\code{backend_meta}}{A named list of diagnostic information.
Standard fields populated by the dispatcher:
\describe{
\item{\code{n_queried}}{Unique input species count.}
\item{\code{n_requested}}{The \code{n_tree} argument the user passed.}
\item{\code{n_returned}}{Number of trees in \code{tree} (1 for \code{phylo}).}
\item{\code{n_matched}}{Equal to \code{length(matched)}.}
\item{\code{tnrs_replacements}}{When TNRS ran (\code{tnrs = "always"},
or \code{tnrs = "auto"} for \code{fishtree}) and \code{rotl} is installed:
a named character vector mapping original input to the
TNRS-resolved name, for names that TNRS changed. \code{NULL}
when no TNRS or no replacements occurred. A one-shot \code{cli}
warning lists the first three substitutions on the call,
so silent name correction is impossible.}
\item{\code{tip_set_consistent}}{Logical. For \code{multiPhylo} returns:
\code{TRUE} if every tree shares the same tip set.}
\item{\code{dropped_per_tree}}{For \code{multiPhylo} returns where
\code{tip_set_consistent = FALSE}: a list of character vectors,
per tree, listing species missing from each tree relative
to the union of all trees. \code{NULL} otherwise.}
\item{\code{tree_provenance}}{A list with one entry per returned
tree (so \code{tree[[i]]} pairs with
\code{backend_meta$tree_provenance[[i]]} when \code{tree} is a
\code{multiPhylo}).}
}
Backend-specific fields (e.g. \code{taxon}, \code{n_grafted},
\code{grafted_tips}, \code{placement} for \code{rtrees}; \code{backend}, \code{type},
\code{tnrs_table} for \code{fishtree} / \code{rotl}; \code{summary_format},
\code{source_citations}, \code{reference} for \code{datelife}) are merged in
at the top level by the wrapper that called the backend. The
\code{rtrees}-specific \code{placement} slot is a tibble with one row per
unique input species and columns \code{input_name}, \code{tree_name},
\code{placement_status} (\code{"exact"}, \code{"genus_added"},
\code{"family_added"}, \code{"skipped"}, or \code{"unmatched"}).}
}
}
\description{
Connects reconciled species names to an external phylogenetic resource
and returns a pruned candidate tree plus a report of which species
were matched and which were dropped. Intended as the bridge between
the package's reconciliation cascade and any downstream comparative
analysis: feed the result of \code{reconcile_data()} / \code{reconcile_tree()}
(or any character vector of cleaned names) into \code{pr_get_tree()} and
get back a \code{phylo} ready for \code{reconcile_apply()}.
}
\details{
Each backend is provided by an external R package that we list in
\code{Suggests} rather than \code{Imports}, so installing \strong{prepR4pcm} does
not pull them in automatically. The error message tells you what
to install if you ask for a backend you don't have.

\strong{Name handling.} Input names are run through
\code{\link[=pr_normalize_names]{pr_normalize_names()}} before the backend is queried --- underscores
become spaces, leading/trailing whitespace is trimmed, OTT-id
suffixes (e.g. \code{ott770315}) and authority strings (e.g.
\verb{(Linnaeus, 1758)}) are stripped, and hybrid signs are
standardised. The \code{matched} and \code{unmatched} slots in the result use
the \emph{original} input format (as you typed it), not the normalised
form.

When TNRS substitutes a name (only when \code{tnrs = "always"}, or for
the \code{fishtree} backend under \code{tnrs = "auto"}), the replacement is
recorded in \code{result$backend_meta$tnrs_replacements} as a named
character vector \code{(original = resolved)}. A one-shot \code{cli} warning
lists the first few substitutions on the call itself.

TNRS also returns structured match metadata. \code{pr_get_tree()} records
it per name in the \code{mapping} tibble: \code{tnrs_number_matches},
\code{tnrs_is_synonym}, \code{tnrs_approximate_match}, and \code{tnrs_flags}. When a
name resolves to more than one taxon (\code{tnrs_number_matches > 1}, a
homonym), a one-shot \code{cli} warning names the affected species, since
the resolved name is then only one of several candidates.
}
\examples{
if (interactive()) {
  # Example 1: birds via clootl (Clements taxonomy). Uses the
  # bundled AVONET subset (657 species placed in the Clements tree).
  data(avonet_subset)
  if (requireNamespace("clootl", quietly = TRUE)) {
    res <- pr_get_tree(avonet_subset, species_col = "Species1",
                       source = "clootl")
    ape::Ntip(res$tree)        # species placed in the tree
    head(res$unmatched)        # names clootl could not resolve
  }

  # Example 2: fish via fishtree (Rabosky et al. 2018, time-calibrated)
  if (requireNamespace("fishtree", quietly = TRUE)) {
    res <- pr_get_tree(c("Salmo salar", "Esox lucius", "Gadus morhua"),
                       source = "fishtree")
    res$tree
  }

  # Example 3: anything via rotl (universal, network)
  if (requireNamespace("rotl", quietly = TRUE)) {
    res <- pr_get_tree(c("Homo sapiens", "Pan troglodytes",
                         "Mus musculus"),
                       source = "rotl")
    res$tree
  }

  # Example 4: posterior of fish trees (50 trees, for multi-tree PCMs)
  if (requireNamespace("fishtree", quietly = TRUE)) {
    res <- pr_get_tree(c("Salmo salar", "Esox lucius"),
                       source = "fishtree", n_tree = 50)
    class(res$tree)            # "multiPhylo"
  }
}

}
\references{
Backend reference trees:

Jetz, W., Thomas, G. H., Joy, J. B., Hartmann, K., & Mooers, A. O.
(2012). The global diversity of birds in space and time.
\emph{Nature} 491: 444--448. \doi{10.1038/nature11631}
(Used by \code{rtrees} for \code{taxon = "bird"} and by BirdTree.)

Rabosky, D. L., Chang, J., Title, P. O., Cowman, P. F., Sallan, L.,
Friedman, M., Kaschner, K., Garilao, C., Near, T. J., Coll, M., &
Alfaro, M. E. (2018). An inverse latitudinal gradient in speciation
rate for marine fishes. \emph{Nature} 559: 392--395.
\doi{10.1038/s41586-018-0273-1}
(Fish Tree of Life; used by \code{source = "fishtree"} and by \code{rtrees}
for \code{taxon = "fish"}.)

Upham, N. S., Esselstyn, J. A., & Jetz, W. (2019). Inferring the
mammal tree: Species-level sets of phylogenies for questions in
ecology, evolution, and conservation. \emph{PLOS Biology} 17(12):
e3000494. \doi{10.1371/journal.pbio.3000494}
(VertLife mammal posterior; used by \code{rtrees} for \code{taxon = "mammal"}
with \code{mammal_tree = "vertlife"}.)

Jin, Y. & Qian, H. (2019). V.PhyloMaker: an R package that can
generate very large phylogenies for vascular plants.
\emph{Ecography} 42(8): 1353--1359. \doi{10.1111/ecog.04434}
(Vascular-plant mega-tree used by \code{rtrees} for \code{taxon = "plant"};
also the basis for the \code{source = "vphylomaker"} augmentation
backend in \code{\link[=reconcile_augment]{reconcile_augment()}}.)

Sanchez Reyes, L. L., O'Meara, B. C., Brown, J. W., & McTavish, E.
J. (2024). DateLife: Leveraging databases and analytical tools to
reveal the dated Tree of Life. \emph{Systematic Biology} 73(2):
470--485. \doi{10.1093/sysbio/syae015}
(Used by \code{source = "datelife"} and by \code{pr_date_tree()}.)

Methodology:

Chang, J., Rabosky, D. L., & Alfaro, M. E. (2019). Estimating
diversification rates on incompletely sampled phylogenies:
Theoretical concerns and practical solutions. \emph{Systematic Biology}
69(3): 602--611. \doi{10.1093/sysbio/syz081}
(Stochastic polytomy resolution behind \code{fishtree_complete_phylogeny()}
for \code{n_tree > 1}.)

Michonneau, F., Brown, J. W., & Winter, D. J. (2016). rotl: an R
package to interact with the Open Tree of Life data. \emph{Methods in
Ecology and Evolution} 7(12): 1476--1481.
\doi{10.1111/2041-210X.12593}
(TNRS preflight and \code{source = "rotl"}.)
}
\seealso{
\code{\link[=reconcile_tree]{reconcile_tree()}} / \code{\link[=reconcile_data]{reconcile_data()}} for producing the
reconciled species list that feeds this function;
\code{\link[=reconcile_apply]{reconcile_apply()}} for combining the returned \code{phylo} with the
data frame ready for analysis;
\code{\link[=reconcile_augment]{reconcile_augment()}} for filling gaps in an existing tree
(a tree-aware alternative to retrieving a fresh tree);
\code{\link[=pr_date_tree]{pr_date_tree()}} for time-calibrating an existing topology;
\code{\link[=pr_cite_tree]{pr_cite_tree()}} for formatting citations for a tree result;
\code{\link[=pr_tree_compare]{pr_tree_compare()}} for comparing two or more retrieved trees;
\code{\link[=pr_get_tree_status]{pr_get_tree_status()}} for checking which backends are installed
and reachable;
\code{\link[=pr_tree_cache_dir]{pr_tree_cache_dir()}} / \code{\link[=pr_tree_cache_status]{pr_tree_cache_status()}} /
\code{\link[=pr_tree_cache_clear]{pr_tree_cache_clear()}} for managing the on-disk cache.
The companion package
\href{https://itchyshin.github.io/pigauto/}{pigauto} consumes a
\code{multiPhylo} directly via \code{multi_impute_trees()} for posterior-
tree PCMs --- request a posterior sample with \code{n_tree > 1}.
}
