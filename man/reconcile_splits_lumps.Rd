% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pr_splits_lumps.R
\name{reconcile_splits_lumps}
\alias{reconcile_splits_lumps}
\title{Flag taxonomic splits and lumps in a reconciliation}
\usage{
reconcile_splits_lumps(reconciliation, quiet = FALSE)
}
\arguments{
\item{reconciliation}{A \link{reconciliation} object built with a
non-\code{NULL} \code{authority} argument. The function inspects the
\code{name_resolved} column, which is only populated when synonym
resolution was performed.}

\item{quiet}{Logical. Suppresses the console summary when \code{TRUE}.
Default \code{FALSE}.}
}
\value{
Invisibly, a list with two tibbles:
\describe{
\item{\code{splits}}{Cases where one name in source \code{x} corresponds
to multiple accepted names in source \code{y}.}
\item{\code{lumps}}{Cases where several names in source \code{x} share a
single accepted name in source \code{y}.}
}
}
\description{
Taxonomic revisions often split a single species into several or
lump several into one. When your data and your reference taxonomy
disagree on such cases, the reconciliation mapping will show one
name in one source linked to multiple accepted names in the other.
\code{reconcile_splits_lumps()} scans a \link{reconciliation} for these cases
and returns them as two tibbles, one for splits and one for lumps,
so you can decide how to handle each before running your PCM
(e.g. keep only one of the split taxa, pool traits across a lumped
set, or exclude them entirely).
}
\details{
Detection relies on the \code{name_resolved} column populated by
synonym resolution --- so \code{authority} must have been set (i.e. not
\code{NULL}) when building the reconciliation.
}
\examples{
# `reconcile_splits_lumps()` only surfaces rows that synonym lookup
# resolved (`match_type == "synonym"`), which requires `authority`
# to be non-NULL when building the reconciliation. The bundled-data
# call below uses `authority = NULL` for speed, so the output is
# empty:
data(avonet_subset)
data(tree_jetz)
rec <- reconcile_tree(avonet_subset, tree_jetz,
                      x_species = "Species1", authority = NULL,
                      quiet = TRUE)
sl <- reconcile_splits_lumps(rec, quiet = TRUE)
nrow(sl$splits); nrow(sl$lumps)   # 0 and 0

# To show what the output looks like when splits and lumps DO turn
# up, we hand-build a tiny reconciliation. In practice you would
# obtain this by calling reconcile_tree(..., authority = "col").
#
#   * Acanthiza pusilla (data) was split in CoL into A. pusilla and
#     A. apicalis  (1 x-name -> 2 y-names  ==>  split).
#   * Parus caeruleus and Cyanistes caeruleus (data: old + new names)
#     both map to Cyanistes caeruleus in CoL
#                 (2 x-names -> 1 y-name  ==>  lump).
demo_mapping <- tibble::tibble(
  name_x        = c("Acanthiza pusilla", "Acanthiza pusilla",
                    "Parus caeruleus",   "Cyanistes caeruleus"),
  name_y        = c("Acanthiza pusilla", "Acanthiza apicalis",
                    "Cyanistes caeruleus", "Cyanistes caeruleus"),
  name_resolved = c("Acanthiza pusilla", "Acanthiza pusilla",
                    "Cyanistes caeruleus", "Cyanistes caeruleus"),
  match_type    = "synonym",
  match_score   = 1,
  match_source  = "col",
  in_x          = TRUE,
  in_y          = TRUE,
  notes         = NA_character_
)
rec_demo <- structure(
  list(mapping   = demo_mapping,
       meta      = list(type = "data_tree", authority = "col"),
       counts    = list(),
       overrides = tibble::tibble()),
  class = "reconciliation"
)
sl <- reconcile_splits_lumps(rec_demo, quiet = TRUE)
sl$splits     # 1 row: Acanthiza pusilla split into 2 taxa
sl$lumps      # 1 row: Parus + Cyanistes lumped into 1 taxon

}
\seealso{
\code{\link[=reconcile_diff]{reconcile_diff()}} for comparing two reconciliations,
which surfaces the same splits/lumps across taxonomy versions.

Other reconciliation functions:
\code{\link[=reconcile_apply]{reconcile_apply()}},
\code{\link[=reconcile_augment]{reconcile_augment()}},
\code{\link[=reconcile_crosswalk]{reconcile_crosswalk()}},
\code{\link[=reconcile_crosswalk_supplement]{reconcile_crosswalk_supplement()}},
\code{\link[=reconcile_data]{reconcile_data()}},
\code{\link[=reconcile_diff]{reconcile_diff()}},
\code{\link[=reconcile_export]{reconcile_export()}},
\code{\link[=reconcile_mapping]{reconcile_mapping()}},
\code{\link[=reconcile_merge]{reconcile_merge()}},
\code{\link[=reconcile_multi]{reconcile_multi()}},
\code{\link[=reconcile_override]{reconcile_override()}},
\code{\link[=reconcile_override_batch]{reconcile_override_batch()}},
\code{\link[=reconcile_plot]{reconcile_plot()}},
\code{\link[=reconcile_report]{reconcile_report()}},
\code{\link[=reconcile_review]{reconcile_review()}},
\code{\link[=reconcile_suggest]{reconcile_suggest()}},
\code{\link[=reconcile_summary]{reconcile_summary()}},
\code{\link[=reconcile_to_trees]{reconcile_to_trees()}},
\code{\link[=reconcile_tree]{reconcile_tree()}},
\code{\link[=reconcile_trees]{reconcile_trees()}}
}
\concept{reconciliation functions}
