% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_data.R
\name{reconcile_data}
\alias{reconcile_data}
\title{Reconcile species names between two datasets}
\usage{
reconcile_data(
  x,
  y,
  x_species = NULL,
  y_species = NULL,
  authority = "col",
  rank = c("species", "subspecies"),
  overrides = NULL,
  db_version = NULL,
  fuzzy = FALSE,
  fuzzy_threshold = 0.9,
  flag_threshold = 0.95,
  resolve = c("flag", "first"),
  quiet = FALSE,
  x_label = NULL,
  y_label = NULL
)
}
\arguments{
\item{x}{A data frame whose species will be matched \emph{from}.}

\item{y}{A data frame whose species will be matched \emph{to}
(typically the "reference" taxonomy or the dataset you want to
merge with).}

\item{x_species}{A length-1 character vector. Name of the column in
\code{x} containing scientific names. Auto-detected (e.g. \code{species},
\code{Species1}, \code{scientific_name}) when \code{NULL}.}

\item{y_species}{A length-1 character vector. Name of the column in
\code{y} containing scientific names. Auto-detected when \code{NULL}.}

\item{authority}{A length-1 character vector, or \code{NULL}. Taxonomic
authority used for synonym resolution (stage 3 of the cascade).
One of:
\describe{
\item{\code{"col"} (default)}{Catalogue of Life --- broad, curated,
frequently updated. A sensible default for most taxa.}
\item{\code{"itis"}}{Integrated Taxonomic Information System ---
strong for North American vertebrates and plants.}
\item{\code{"gbif"}}{Global Biodiversity Information Facility
backbone. Wider coverage; includes more recent synonymy.}
\item{\code{"ncbi"}}{NCBI Taxonomy --- best when working with
sequence data.}
\item{\code{"ott"}}{Open Tree of Life synthetic taxonomy. Useful when
your downstream phylogeny is from the Open Tree synthesis.}
\item{\code{"itis_test"}}{A small bundled subset of ITIS, cached
locally with \pkg{taxadb} for testing. Intended for examples
and unit tests; not for analysis.}
\item{\code{"gnverifier"}}{HTTP-backed verification against ~100
sources via the Global Names verifier; no local database
download. See \code{vignette("getting-started")} for the
trade-off (wider coverage, requires network and the
\pkg{httr2} package).}
\item{\code{NULL}}{Skip the synonym stage entirely. Useful for quick
checks or when \pkg{taxadb} is unavailable. Stages 1, 2 and 4
still run.}
}

Five authority codes that earlier versions of the package
advertised --- \code{"iucn"}, \code{"tpl"}, \code{"fb"}, \code{"slb"}, \code{"wd"} --- are
no longer accepted. Empirical testing against \pkg{taxadb} v22.12
showed that \code{iucn} errors with a schema mismatch and the others
are not \pkg{taxadb} providers at all. Passing one of those
values now produces a helpful migration error.}

\item{rank}{A length-1 character vector. Controls how trinomials
are handled during normalisation:
\describe{
\item{\code{"species"} (default)}{Strip infraspecific epithets so
that \code{"Parus major major"} becomes \code{"Parus major"} before
matching.}
\item{\code{"subspecies"}}{Keep trinomials intact. Use this when
your analysis operates at subspecies level.}
}}

\item{overrides}{Optional pre-built corrections. Either a data
frame with at least columns \code{name_x} and \code{name_y} (plus an
optional \code{user_note} column), or a file path to a CSV with the
same columns. Any name listed here bypasses the cascade and is
recorded as \code{match_type = "manual"}. Use this for locked decisions.
For published crosswalks (see \code{\link[=reconcile_crosswalk]{reconcile_crosswalk()}}), prefer
\code{\link[=reconcile_crosswalk_supplement]{reconcile_crosswalk_supplement()}} or \code{one_to_one_only = TRUE} for
automatic use and review split/lump cases before applying them.}

\item{db_version}{A length-1 character vector. \pkg{taxadb}
database snapshot to use (e.g. \code{"22.12"}). \code{NULL} (default) uses
the latest available.}

\item{fuzzy}{Logical. Enables the fuzzy-matching stage when
\code{TRUE}. Default \code{FALSE}. Turn this on to catch likely typos
(\emph{Corvus brachyrhnchos} -> \emph{Corvus brachyrhynchos}). When
\code{FALSE}, stages 1--3 still run.}

\item{fuzzy_threshold}{Numeric in [0, 1]. Minimum genus-weighted
similarity score for a fuzzy match to be accepted. Default \code{0.9}
(roughly "no more than ~10\% of characters differ"). Lower values
(e.g. \code{0.7}) are more permissive but produce more false
positives; always review fuzzy matches with \code{\link[=reconcile_suggest]{reconcile_suggest()}}
or \code{\link[=reconcile_review]{reconcile_review()}} before trusting them.}

\item{flag_threshold}{Numeric in [0, 1]. When \code{resolve = "flag"},
fuzzy matches with a score below this value are recorded as
\code{match_type = "flagged"} rather than \code{"fuzzy"}, marking them for
manual review. Default \code{0.95}. Must be >= \code{fuzzy_threshold} to
have any effect.}

\item{resolve}{A length-1 character vector. What to do with
borderline matches:
\describe{
\item{\code{"flag"} (default)}{Mark low-confidence fuzzy matches
(score below \code{flag_threshold}) and names with indirect
taxadb synonymy as \code{match_type = "flagged"} so you can audit
them with \code{\link[=reconcile_review]{reconcile_review()}} or \code{\link[=reconcile_suggest]{reconcile_suggest()}}.}
\item{\code{"first"}}{Accept the highest-scoring candidate silently,
without flagging. Faster but riskier; use only when you have
already reviewed the ambiguities.}
}}

\item{quiet}{Logical. Suppresses progress messages when \code{TRUE}.
Default \code{FALSE}.}

\item{x_label}{A length-1 character vector or \code{NULL}. Human-readable label for source \code{x}
stored in the reconciliation metadata and shown in \code{print()} / \code{format()}.
Defaults to the expression passed as \code{x} (via \code{deparse(substitute())}).
Set this explicitly when calling \code{reconcile_data()} inside another function
so the label reflects the real data source rather than the local argument
name.}

\item{y_label}{A length-1 character vector or \code{NULL}. Same as \code{x_label}, for source \code{y}.}
}
\value{
A \link{reconciliation} object. The accompanying mapping tibble,
match-type counts, provenance metadata, and applied / unused
override slots are documented in \link{reconciliation}. See the
"After the call" section above for the most common next steps.
}
\description{
Match the species column of one data frame (\code{x}) to the species column
of another (\code{y}), returning a \link{reconciliation} object that records how
every name was resolved. Use this when combining trait datasets, range
datasets, or any other species-level tables that may use slightly
different taxonomies or spellings.
}
\details{
Names are passed through a four-stage matching cascade, and the first
stage that returns a match is recorded in \code{match_type}:

\enumerate{
\item \strong{exact} --- verbatim string equality.
\item \strong{normalized} --- after stripping underscores, authority
strings (\emph{"Corvus corax Linnaeus, 1758"}), diacritics, and
case/whitespace differences.
\item \strong{synonym} --- lookup in a local taxonomic database via
\pkg{taxadb} (Catalogue of Life, GBIF, ITIS, NCBI, ...). Skipped if
\code{authority = NULL}.
\item \strong{fuzzy} --- character-level similarity (opt-in via
\code{fuzzy = TRUE}). Uses a genus-weighted Levenshtein score
(60\% genus, 40\% specific epithet) with a genus pre-filter so that
only plausibly similar genera are compared.
}

Names that survive all four stages are labelled \code{unresolved}. Any
entries supplied through \code{overrides} take precedence over the cascade.
This is intentional for locked manual decisions, but it also means a
crosswalk row can preempt an otherwise exact or normalised match. If you
want a crosswalk to supplement, rather than replace, the baseline cascade,
use \code{\link[=reconcile_crosswalk_supplement]{reconcile_crosswalk_supplement()}} or run the baseline reconciliation
first and apply reviewed overrides only to still-unresolved names.

\strong{After the call.} A \code{reconciliation} object is the input to
most other functions in the package. Common next steps:

\itemize{
\item \code{\link[=reconcile_summary]{reconcile_summary()}} --- human-readable breakdown of matches.
\item \code{\link[=reconcile_plot]{reconcile_plot()}} --- one-glance bar/pie of match composition.
\item \code{\link[=reconcile_mapping]{reconcile_mapping()}} --- extract the full per-name tibble.
\item \code{\link[=reconcile_suggest]{reconcile_suggest()}} --- near-miss candidates for unresolved
names.
\item \code{\link[=reconcile_merge]{reconcile_merge()}} --- join the two datasets using the
reconciliation as the species key.
\item \code{\link[=reconcile_report]{reconcile_report()}} --- shareable HTML audit trail.
}
}
\examples{
# Merge AVONET morphology with nest-site data. Both datasets use
# slightly different taxonomies; authority = NULL keeps the example
# offline (no taxadb download).
data(avonet_subset)
data(nesttrait_subset)

rec <- reconcile_data(avonet_subset, nesttrait_subset,
                      x_species = "Species1",
                      y_species = "Scientific_name",
                      authority = NULL)
rec                      # concise print method
reconcile_summary(rec)   # full breakdown

# Join the two datasets on the reconciled species key
merged <- reconcile_merge(rec, avonet_subset, nesttrait_subset,
                          species_col_x = "Species1",
                          species_col_y = "Scientific_name")
head(merged[, c("species_resolved", "Family1", "Common_name")])

}
\references{
Norman, K.E., Chamberlain, S. & Boettiger, C. (2020) taxadb: A
high-performance local taxonomic database interface.
\emph{Methods in Ecology and Evolution} 11:1153--1159.
\doi{10.1111/2041-210X.13440}
}
\seealso{
\code{\link[=reconcile_tree]{reconcile_tree()}} for matching against a phylogenetic tree;
\code{\link[=reconcile_to_trees]{reconcile_to_trees()}} / \code{\link[=reconcile_trees]{reconcile_trees()}} / \code{\link[=reconcile_multi]{reconcile_multi()}}
for multi-input workflows.

Other reconciliation functions:
\code{\link[=reconcile_apply]{reconcile_apply()}},
\code{\link[=reconcile_augment]{reconcile_augment()}},
\code{\link[=reconcile_crosswalk]{reconcile_crosswalk()}},
\code{\link[=reconcile_crosswalk_supplement]{reconcile_crosswalk_supplement()}},
\code{\link[=reconcile_diff]{reconcile_diff()}},
\code{\link[=reconcile_export]{reconcile_export()}},
\code{\link[=reconcile_mapping]{reconcile_mapping()}},
\code{\link[=reconcile_merge]{reconcile_merge()}},
\code{\link[=reconcile_multi]{reconcile_multi()}},
\code{\link[=reconcile_override]{reconcile_override()}},
\code{\link[=reconcile_override_batch]{reconcile_override_batch()}},
\code{\link[=reconcile_plot]{reconcile_plot()}},
\code{\link[=reconcile_report]{reconcile_report()}},
\code{\link[=reconcile_review]{reconcile_review()}},
\code{\link[=reconcile_splits_lumps]{reconcile_splits_lumps()}},
\code{\link[=reconcile_suggest]{reconcile_suggest()}},
\code{\link[=reconcile_summary]{reconcile_summary()}},
\code{\link[=reconcile_to_trees]{reconcile_to_trees()}},
\code{\link[=reconcile_tree]{reconcile_tree()}},
\code{\link[=reconcile_trees]{reconcile_trees()}}
}
\concept{reconciliation functions}
