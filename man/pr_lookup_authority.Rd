% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pr_authority.R
\name{pr_lookup_authority}
\alias{pr_lookup_authority}
\title{Look up names in a taxonomic authority}
\usage{
pr_lookup_authority(names, authority = "col", db_version = NULL)
}
\arguments{
\item{names}{Character vector of scientific names.}

\item{authority}{A length-1 character vector. Authority code (e.g.,
\code{"col"}). Pass \code{"gnverifier"} for HTTP-backed verification against
~100 sources; see \code{vignette("getting-started")} for the trade-off.}

\item{db_version}{A length-1 character vector or NULL. Ignored when
\code{authority = "gnverifier"} (the GNverifier service does not expose
per-snapshot versions); a non-NULL value emits a single warning.}
}
\value{
A tibble with columns: \code{input}, \code{accepted_name}, \code{status},
\code{taxon_id}, \code{authority}.
}
\description{
For each name, queries the configured authority and returns the accepted
name, taxonomic status, and taxon ID. Most authorities are backed by a
local \pkg{taxadb} database; \code{authority = "gnverifier"} calls the
Global Names HTTP verifier instead.
}
\keyword{internal}
